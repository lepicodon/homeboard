services:
  app:
    build: .
    container_name: hometodo
    restart: unless-stopped
    ports:
      - '3000:3000'
    volumes:
      - ./data:/data
    environment:
      - PORT=3000
      - DB_PATH=/data/todo.db
