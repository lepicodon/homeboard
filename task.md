# Tasks

- `[x]` Component 1: Dependency Updates & Build Config
  - `[x]` Install `compression` and development dependencies (`eslint`, `prettier`, `jest`).
  - `[x]` Configure ESLint and Prettier configs.
  - `[x]` Add lint/format/test scripts to `package.json`.
- `[x]` Component 2: Backend Modularization (`server.js`)
  - `[x]` Create `src/config/db.js` and move DB setup & migrations there.
  - `[x]` Create `src/routes/categories.js` and move task category endpoints.
  - `[x]` Create `src/routes/members.js` and move member endpoints.
  - `[x]` Create `src/routes/tasks.js` and move task endpoints, toggle endpoint, and recurrence helper functions.
  - `[x]` Create `src/routes/memos.js` and move sticky note memo endpoints.
  - `[x]` Create `src/routes/shopping.js` and move shopping list, category, and items endpoints.
  - `[x]` Create `src/routes/settings.js` and move settings and weather endpoints. Implement API key masking in GET and safe handling in PUT.
  - `[x]` Modify `server.js` to integrate all routers and `compression` middleware.
- `[x]` Component 3: Frontend Modularization (`app.js`)
  - `[x]` Create `public/js/state.js` to hold the shared frontend state.
  - `[x]` Create `public/js/api.js` to house all the consolidated network `fetch` requests.
  - `[x]` Create `public/js/modules/tasks.js` to handle rendering, modals, and filters for tasks.
  - `[x]` Create `public/js/modules/calendar.js` to render the calendar view and agenda items.
  - `[x]` Create `public/js/modules/memos.js` to render sticky notes and memo modals.
  - `[x]` Create `public/js/modules/shopping.js` to render shopping list view, lists selector, and clearing actions.
  - `[x]` Create `public/js/modules/settings.js` to render categories list, members list, and form inputs for configurations.
  - `[x]` Modify `public/js/app.js` to serve as the entry point and router.
  - `[x]` Update `public/index.html` script tag to `type="module"`.
- `[x]` Component 4: Local Font Self-Hosting (Performance)
  - `[x]` Create `public/fonts/` directory and download the Outfit font files in `.woff2` (or `.ttf`) format.
  - `[x]` Update `public/index.html` to point to a local font stylesheet and remove remote Google Fonts links.
  - `[x]` Add local font `@font-face` rules.
- `[ ]` Multiple Weather Locations Support
  - `[ ]` Update database schema in `src/config/db.js` with `weather_locations` table and data migration logic.
  - `[ ]` Add weather location routes (`GET /api/weather/locations`, `POST`, `DELETE`) and update `GET /api/weather` in `src/routes/settings.js`.
  - `[ ]` Update `public/index.html` to add location dropdown selector, Add/Delete buttons, modal dialog, and clean settings page layout.
  - `[ ]` Add weather location variables to `public/js/state.js`.
  - `[ ]` Add API routes to `public/js/api.js`.
  - `[ ]` Create `public/js/modules/weather.js` to handle location selectors, modals, additions, and deletions.
  - `[ ]` Clean up settings events in `public/js/modules/settings.js` and orchestrator in `public/js/app.js`.
- `[ ]` Verification and Testing
  - `[ ]` Run linter and formatter.
  - `[ ]` Add Jest test for weather locations CRUD and execute the test suite.
  - `[ ]` Start server and verify weather page functionalities.
